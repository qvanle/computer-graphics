// the file is use to test the window class
#include "window.hpp"

int main() {
    // Create a fullscreen window with a title
    Window window("Fullscreen Test");
    
    // Run the window loop
    window.run();

    return 0;
}

